function parImpar(){
    var num = Number(prompt("Inserir número:"));

    if (num % 2 == 0) {
        alert("O número inserido " + num + " é ímpar.");
    } else {
        alert("O número inserido " + num + " é par.");
    }

}

parImpar();
parImpar();
parImpar();